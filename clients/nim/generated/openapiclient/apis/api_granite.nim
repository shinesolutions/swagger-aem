#
# Adobe Experience Manager (AEM) API
# 
# Swagger AEM is an OpenAPI specification for Adobe Experience Manager (AEM) API
# The version of the OpenAPI document: 3.7.1-pre.0
# Contact: opensource@shinesolutions.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri


const basepath = "http://localhost"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc sslSetup*(httpClient: HttpClient, keystorePassword: string, keystorePasswordConfirm: string, truststorePassword: string, truststorePasswordConfirm: string, httpsHostname: string, httpsPort: string, privatekeyFile: string, certificateFile: string): (Option[string], Response) =
  ## 
  httpClient.headers["Content-Type"] = "multipart/form-data"
  var query_params_list: seq[(string, string)] = @[]
  query_params_list.add(("keystorePassword", $keystorePassword))
  query_params_list.add(("keystorePasswordConfirm", $keystorePasswordConfirm))
  query_params_list.add(("truststorePassword", $truststorePassword))
  query_params_list.add(("truststorePasswordConfirm", $truststorePasswordConfirm))
  query_params_list.add(("httpsHostname", $httpsHostname))
  query_params_list.add(("httpsPort", $httpsPort))
  let url_encoded_query_params = encodeQuery(query_params_list)
  let multipart_data = newMultipartData({
    "privatekeyFile": $privatekeyFile, # 
    "certificateFile": $certificateFile, # 
  })

  let response = httpClient.post(basepath & "/libs/granite/security/post/sslSetup.html" & "?" & url_encoded_query_params, multipart=multipart_data)
  constructResult[string](response)

