/**
 * Adobe Experience Manager (AEM) API
 * Swagger AEM is an OpenAPI specification for Adobe Experience Manager (AEM) API
 *
 * The version of the OpenAPI document: 3.7.1-pre.0
 * Contact: opensource@shinesolutions.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include <QJsonArray>
#include <QJsonDocument>
#include <QJsonObject>
#include <QVariantMap>


#include "OAIApiRouter.h"
#include "OAIConsoleApiRequest.h"
#include "OAICqApiRequest.h"
#include "OAICrxApiRequest.h"
#include "OAICustomApiRequest.h"
#include "OAIGraniteApiRequest.h"
#include "OAISlingApiRequest.h"


namespace OpenAPI {

OAIApiRouter::OAIApiRouter() {
    createApiHandlers();
}

OAIApiRouter::~OAIApiRouter(){

}

void OAIApiRouter::createApiHandlers() { 
    mOAIConsoleApiHandler = QSharedPointer<OAIConsoleApiHandler>::create();
    mOAICqApiHandler = QSharedPointer<OAICqApiHandler>::create();
    mOAICrxApiHandler = QSharedPointer<OAICrxApiHandler>::create();
    mOAICustomApiHandler = QSharedPointer<OAICustomApiHandler>::create();
    mOAIGraniteApiHandler = QSharedPointer<OAIGraniteApiHandler>::create();
    mOAISlingApiHandler = QSharedPointer<OAISlingApiHandler>::create();
}


void OAIApiRouter::setOAIConsoleApiHandler(QSharedPointer<OAIConsoleApiHandler> handler){
    mOAIConsoleApiHandler = handler;
}
void OAIApiRouter::setOAICqApiHandler(QSharedPointer<OAICqApiHandler> handler){
    mOAICqApiHandler = handler;
}
void OAIApiRouter::setOAICrxApiHandler(QSharedPointer<OAICrxApiHandler> handler){
    mOAICrxApiHandler = handler;
}
void OAIApiRouter::setOAICustomApiHandler(QSharedPointer<OAICustomApiHandler> handler){
    mOAICustomApiHandler = handler;
}
void OAIApiRouter::setOAIGraniteApiHandler(QSharedPointer<OAIGraniteApiHandler> handler){
    mOAIGraniteApiHandler = handler;
}
void OAIApiRouter::setOAISlingApiHandler(QSharedPointer<OAISlingApiHandler> handler){
    mOAISlingApiHandler = handler;
}

void OAIApiRouter::setUpRoutes() {
    
    Routes.insert(QString("%1 %2").arg("GET").arg("/system/console/status-productinfo.json").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIConsoleApiRequest(socket, mOAIConsoleApiHandler);
            reqObj->getAemProductInfoRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/system/console/configMgr").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIConsoleApiRequest(socket, mOAIConsoleApiHandler);
            reqObj->getConfigMgrRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/system/console/configMgr/com.adobe.granite.auth.saml.SamlAuthenticationHandler").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIConsoleApiRequest(socket, mOAIConsoleApiHandler);
            reqObj->postSamlConfigurationRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/libs/granite/core/content/login.html").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAICqApiRequest(socket, mOAICqApiHandler);
            reqObj->getLoginPageRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/.cqactions.html").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAICqApiRequest(socket, mOAICqApiHandler);
            reqObj->postCqActionsRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/crx/server/crx.default/jcr:root/.1.json").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAICrxApiRequest(socket, mOAICrxApiHandler);
            reqObj->getCrxdeStatusRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/crx/packmgr/installstatus.jsp").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAICrxApiRequest(socket, mOAICrxApiHandler);
            reqObj->getInstallStatusRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/crx/packmgr/service/script.html").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAICrxApiRequest(socket, mOAICrxApiHandler);
            reqObj->getPackageManagerServletRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/crx/packmgr/service.jsp").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAICrxApiRequest(socket, mOAICrxApiHandler);
            reqObj->postPackageServiceRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/crx/packmgr/update.jsp").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAICrxApiRequest(socket, mOAICrxApiHandler);
            reqObj->postPackageUpdateRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/crx/explorer/ui/setpassword.jsp").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAICrxApiRequest(socket, mOAICrxApiHandler);
            reqObj->postSetPasswordRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/system/health").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAICustomApiRequest(socket, mOAICustomApiHandler);
            reqObj->getAemHealthCheckRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/apps/system/config/com.shinesolutions.healthcheck.hc.impl.ActiveBundleHealthCheck").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAICustomApiRequest(socket, mOAICustomApiHandler);
            reqObj->postConfigAemHealthCheckServletRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/apps/system/config/com.shinesolutions.aem.passwordreset.Activator").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAICustomApiRequest(socket, mOAICustomApiHandler);
            reqObj->postConfigAemPasswordResetRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/libs/granite/security/post/sslSetup.html").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAIGraniteApiRequest(socket, mOAIGraniteApiHandler);
            reqObj->sslSetupRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/bin/querybuilder.json").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
            reqObj->getQueryRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/etc/truststore/truststore.p12").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
            reqObj->getTruststoreRequest();
    });
    Routes.insert(QString("%1 %2").arg("GET").arg("/libs/granite/security/truststore.json").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
            reqObj->getTruststoreInfoRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/libs/granite/security/post/authorizables").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
            reqObj->postAuthorizablesRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/apps/system/config/com.adobe.granite.auth.saml.SamlAuthenticationHandler.config").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
            reqObj->postConfigAdobeGraniteSamlAuthenticationHandlerRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/apps/system/config/org.apache.felix.http").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
            reqObj->postConfigApacheFelixJettyBasedHttpServiceRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/apps/system/config/org.apache.http.proxyconfigurator.config").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
            reqObj->postConfigApacheHttpComponentsProxyConfigurationRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/apps/system/config/org.apache.sling.jcr.davex.impl.servlets.SlingDavExServlet").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
            reqObj->postConfigApacheSlingDavExServletRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/apps/system/config/org.apache.sling.servlets.get.DefaultGetServlet").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
            reqObj->postConfigApacheSlingGetServletRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/apps/system/config/org.apache.sling.security.impl.ReferrerFilter").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
            reqObj->postConfigApacheSlingReferrerFilterRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/bin/querybuilder.json").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
            reqObj->postQueryRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/libs/replication/treeactivation.html").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
            reqObj->postTreeActivationRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/libs/granite/security/post/truststore").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
            reqObj->postTruststoreRequest();
    });
    Routes.insert(QString("%1 %2").arg("POST").arg("/etc/truststore").toLower(), [this](QHttpEngine::Socket *socket) {
            auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
            reqObj->postTruststorePKCS12Request();
    });
}

void OAIApiRouter::processRequest(QHttpEngine::Socket *socket){
    if( handleRequest(socket) ){
        return;
    }
    if( handleRequestAndExtractPathParam(socket) ){
        return;
    }
    socket->setStatusCode(QHttpEngine::Socket::NotFound);
    if(socket->isOpen()){
        socket->writeHeaders();
        socket->close();
    }
}

bool OAIApiRouter::handleRequest(QHttpEngine::Socket *socket){
    auto reqPath = QString("%1 %2").arg(fromQHttpEngineMethod(socket->method())).arg(socket->path()).toLower();
    if ( Routes.contains(reqPath) ) {
        Routes.value(reqPath).operator()(socket);
        return true;
    }
    return false;
}

bool OAIApiRouter::handleRequestAndExtractPathParam(QHttpEngine::Socket *socket){
    auto reqPath = QString("%1 %2").arg(fromQHttpEngineMethod(socket->method())).arg(socket->path()).toLower();
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/system/console/bundles/{name}.json").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString name = match.captured(QString("name").toLower());
                auto reqObj = new OAIConsoleApiRequest(socket, mOAIConsoleApiHandler);
                reqObj->getBundleInfoRequest(name);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/system/console/bundles/{name}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString name = match.captured(QString("name").toLower());
                auto reqObj = new OAIConsoleApiRequest(socket, mOAIConsoleApiHandler);
                reqObj->postBundleRequest(name);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/system/console/jmx/com.adobe.granite:type=Repository/op/{action}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString action = match.captured(QString("action").toLower());
                auto reqObj = new OAIConsoleApiRequest(socket, mOAIConsoleApiHandler);
                reqObj->postJmxRepositoryRequest(action);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/crx/packmgr/service/.json/{path}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString path = match.captured(QString("path").toLower());
                auto reqObj = new OAICrxApiRequest(socket, mOAICrxApiHandler);
                reqObj->postPackageServiceJsonRequest(path);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/etc/replication/agents.{runmode}/{name}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString runmode = match.captured(QString("runmode").toLower());
                QString name = match.captured(QString("name").toLower());
                auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
                reqObj->deleteAgentRequest(runmode, name);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("DELETE").arg("/{path}/{name}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString path = match.captured(QString("path").toLower());
                QString name = match.captured(QString("name").toLower());
                auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
                reqObj->deleteNodeRequest(path, name);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/etc/replication/agents.{runmode}/{name}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString runmode = match.captured(QString("runmode").toLower());
                QString name = match.captured(QString("name").toLower());
                auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
                reqObj->getAgentRequest(runmode, name);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/etc/replication/agents.{runmode}.-1.json").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString runmode = match.captured(QString("runmode").toLower());
                auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
                reqObj->getAgentsRequest(runmode);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/{intermediatePath}/{authorizableId}.ks.json").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString intermediatePath = match.captured(QString("intermediatePath").toLower());
                QString authorizableId = match.captured(QString("authorizableId").toLower());
                auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
                reqObj->getAuthorizableKeystoreRequest(intermediatePath, authorizableId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/{intermediatePath}/{authorizableId}/keystore/store.p12").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString intermediatePath = match.captured(QString("intermediatePath").toLower());
                QString authorizableId = match.captured(QString("authorizableId").toLower());
                auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
                reqObj->getKeystoreRequest(intermediatePath, authorizableId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/{path}/{name}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString path = match.captured(QString("path").toLower());
                QString name = match.captured(QString("name").toLower());
                auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
                reqObj->getNodeRequest(path, name);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/etc/packages/{group}/{name}-{version}.zip").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString group = match.captured(QString("group").toLower());
                QString name = match.captured(QString("name").toLower());
                QString version = match.captured(QString("version").toLower());
                auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
                reqObj->getPackageRequest(group, name, version);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("GET").arg("/etc/packages/{group}/{name}-{version}.zip/jcr:content/vlt:definition/filter.tidy.2.json").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString group = match.captured(QString("group").toLower());
                QString name = match.captured(QString("name").toLower());
                QString version = match.captured(QString("version").toLower());
                auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
                reqObj->getPackageFilterRequest(group, name, version);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/etc/replication/agents.{runmode}/{name}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString runmode = match.captured(QString("runmode").toLower());
                QString name = match.captured(QString("name").toLower());
                auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
                reqObj->postAgentRequest(runmode, name);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/{intermediatePath}/{authorizableId}.ks.html").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString intermediatePath = match.captured(QString("intermediatePath").toLower());
                QString authorizableId = match.captured(QString("authorizableId").toLower());
                auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
                reqObj->postAuthorizableKeystoreRequest(intermediatePath, authorizableId);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/apps/system/config/{configNodeName}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString configNodeName = match.captured(QString("configNodeName").toLower());
                auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
                reqObj->postConfigPropertyRequest(configNodeName);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/{path}/{name}").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString path = match.captured(QString("path").toLower());
                QString name = match.captured(QString("name").toLower());
                auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
                reqObj->postNodeRequest(path, name);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/{path}/{name}.rw.html").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString path = match.captured(QString("path").toLower());
                QString name = match.captured(QString("name").toLower());
                auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
                reqObj->postNodeRwRequest(path, name);
                return true;
            }
        }
    }
    {
        auto completePath = QString("%1 %2").arg("POST").arg("/{path}/").toLower();
        if ( reqPath.startsWith(completePath.leftRef( completePath.indexOf(QString("/{")))) ) {
            QRegularExpressionMatch match = getRequestMatch( completePath, reqPath );
            if ( match.hasMatch() ){
                QString path = match.captured(QString("path").toLower());
                auto reqObj = new OAISlingApiRequest(socket, mOAISlingApiHandler);
                reqObj->postPathRequest(path);
                return true;
            }
        }
    }
    return false;
}

}
