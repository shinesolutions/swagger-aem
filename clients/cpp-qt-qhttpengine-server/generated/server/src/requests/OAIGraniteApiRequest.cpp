/**
 * Adobe Experience Manager (AEM) API
 * Swagger AEM is an OpenAPI specification for Adobe Experience Manager (AEM) API
 *
 * The version of the OpenAPI document: 3.7.1-pre.0
 * Contact: opensource@shinesolutions.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include <QJsonArray>
#include <QJsonDocument>
#include <QJsonObject>
#include <QVariantMap>
#include <QDebug>

#include "OAIHelpers.h"
#include "OAIGraniteApiRequest.h"

namespace OpenAPI {

OAIGraniteApiRequest::OAIGraniteApiRequest(QHttpEngine::Socket *s, QSharedPointer<OAIGraniteApiHandler> hdl) : QObject(s), socket(s), handler(hdl) {
    auto headers = s->headers();
    for(auto itr = headers.begin(); itr != headers.end(); itr++) {
        requestHeaders.insert(QString(itr.key()), QString(itr.value()));
    }
}

OAIGraniteApiRequest::~OAIGraniteApiRequest(){
    disconnect(this, nullptr, nullptr, nullptr);
    qDebug() << "OAIGraniteApiRequest::~OAIGraniteApiRequest()";
}

QMap<QString, QString>
OAIGraniteApiRequest::getRequestHeaders() const {
    return requestHeaders;
}

void OAIGraniteApiRequest::setResponseHeaders(const QMultiMap<QString, QString>& headers){
    for(auto itr = headers.begin(); itr != headers.end(); ++itr) {
        responseHeaders.insert(itr.key(), itr.value());
    }
}


QHttpEngine::Socket* OAIGraniteApiRequest::getRawSocket(){
    return socket;
}


void OAIGraniteApiRequest::sslSetupRequest(){
    qDebug() << "/libs/granite/security/post/sslSetup.html";
    connect(this, &OAIGraniteApiRequest::sslSetup, handler.data(), &OAIGraniteApiHandler::sslSetup);

    
    QString keystore_password;
    if(socket->queryString().keys().contains("keystore_password")){
        fromStringValue(socket->queryString().value("keystore_password"), keystore_password);
    }
    
    QString keystore_password_confirm;
    if(socket->queryString().keys().contains("keystore_password_confirm")){
        fromStringValue(socket->queryString().value("keystore_password_confirm"), keystore_password_confirm);
    }
    
    QString truststore_password;
    if(socket->queryString().keys().contains("truststore_password")){
        fromStringValue(socket->queryString().value("truststore_password"), truststore_password);
    }
    
    QString truststore_password_confirm;
    if(socket->queryString().keys().contains("truststore_password_confirm")){
        fromStringValue(socket->queryString().value("truststore_password_confirm"), truststore_password_confirm);
    }
    
    QString https_hostname;
    if(socket->queryString().keys().contains("https_hostname")){
        fromStringValue(socket->queryString().value("https_hostname"), https_hostname);
    }
    
    QString https_port;
    if(socket->queryString().keys().contains("https_port")){
        fromStringValue(socket->queryString().value("https_port"), https_port);
    }
    

    OAIHttpFileElement privatekey_file;
    OAIHttpFileElement certificate_file;

    Q_EMIT sslSetup(keystore_password, keystore_password_confirm, truststore_password, truststore_password_confirm, https_hostname, https_port, privatekey_file, certificate_file);
}



void OAIGraniteApiRequest::sslSetupResponse(const QString& res){
    setSocketResponseHeaders();
    socket->write(::OpenAPI::toStringValue(res).toUtf8());
    if(socket->isOpen()){
        socket->close();
    }
}


void OAIGraniteApiRequest::sslSetupError(const QString& res, QNetworkReply::NetworkError error_type, QString& error_str){
    Q_UNUSED(error_type); // TODO: Remap error_type to QHttpEngine::Socket errors
    setSocketResponseHeaders();
    Q_UNUSED(error_str);  // response will be used instead of error string
    socket->write(::OpenAPI::toStringValue(res).toUtf8());
    if(socket->isOpen()){
        socket->close();
    }
}


void OAIGraniteApiRequest::sendCustomResponse(QByteArray & res, QNetworkReply::NetworkError error_type){
    Q_UNUSED(error_type); // TODO
    socket->write(res);
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIGraniteApiRequest::sendCustomResponse(QIODevice *res, QNetworkReply::NetworkError error_type){
    Q_UNUSED(error_type);  // TODO
    socket->write(res->readAll());
    if(socket->isOpen()){
        socket->close();
    }
}

}
