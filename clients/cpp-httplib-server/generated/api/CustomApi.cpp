/**
* This file is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

// System headers
#include <algorithm>
#include <cctype>
#include <locale>

// Project headers
#include "CustomApi.h"
#include "AuthenticationManager.h"

constexpr int HTTP_RESPONSE_CODE_PRIMITIVE_STRING = 0;
constexpr int HTTP_RESPONSE_CODE_NO_CONTENT = 204;
constexpr int HTTP_RESPONSE_CODE_BAD_REQUEST = 400;
constexpr int HTTP_RESPONSE_CODE_UNAUTHORIZED = 401;
constexpr int HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR = 500;

namespace Api {

using namespace models;

bool Custom::parseSystemhealthGetParams(const httplib::Request& req, Custom::SystemhealthGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - tags
    if (req.has_param("tags"))
    {
        params.m_tags = req.get_param_value("tags");
    }
    else
    {
        // Use default value for optional parameter
        params.m_tags = "";
    }
    // Query Parameters - combineTagsOr
    if (req.has_param("combineTagsOr"))
    {
        try
        {
            params.m_combineTagsOr = (req.get_param_value("combineTagsOr") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'combineTagsOr': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_combineTagsOr = false;
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Custom::handleSystemhealthGetResponse(const SystemhealthGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_PRIMITIVE_STRING;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}
bool Custom::parseAppssystemconfigcomshinesolutionshealthcheckhcimplactiveBundleHealthCheckPostParams(const httplib::Request& req, Custom::AppssystemconfigcomshinesolutionshealthcheckhcimplactiveBundleHealthCheckPostRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - bundles.ignored
    if (req.has_param("bundles.ignored"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("bundles.ignored");
            if (!params.m_bundlesPeriodignored.has_value())
            {
                params.m_bundlesPeriodignored = std::vector<std::string>{};
            }
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("bundles.ignored", i);

                    params.m_bundlesPeriodignored->emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("bundles.ignored", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_bundlesPeriodignored->emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'bundles.ignored': " + std::string(e.what()));
        }
    }
    // Query Parameters - bundles.ignored@TypeHint
    if (req.has_param("bundles.ignored@TypeHint"))
    {
        params.m_bundlesPeriodignoredAtTypeHint = req.get_param_value("bundles.ignored@TypeHint");
    }
    else
    {
        // Use default value for optional parameter
        params.m_bundlesPeriodignoredAtTypeHint = "";
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
bool Custom::parseAppssystemconfigcomshinesolutionsaempasswordresetactivatorPostParams(const httplib::Request& req, Custom::AppssystemconfigcomshinesolutionsaempasswordresetactivatorPostRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - pwdreset.authorizables
    if (req.has_param("pwdreset.authorizables"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("pwdreset.authorizables");
            if (!params.m_pwdresetPeriodauthorizables.has_value())
            {
                params.m_pwdresetPeriodauthorizables = std::vector<std::string>{};
            }
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("pwdreset.authorizables", i);

                    params.m_pwdresetPeriodauthorizables->emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("pwdreset.authorizables", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_pwdresetPeriodauthorizables->emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'pwdreset.authorizables': " + std::string(e.what()));
        }
    }
    // Query Parameters - pwdreset.authorizables@TypeHint
    if (req.has_param("pwdreset.authorizables@TypeHint"))
    {
        params.m_pwdresetPeriodauthorizablesAtTypeHint = req.get_param_value("pwdreset.authorizables@TypeHint");
    }
    else
    {
        // Use default value for optional parameter
        params.m_pwdresetPeriodauthorizablesAtTypeHint = "";
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}

bool Custom::performAuthentication(
    const httplib::Request& req,
    std::shared_ptr<AuthenticationManager> auth,
    httplib::Response& res)
{
    if (!auth)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "AuthenticationManager not configured";
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
        return false;
    }


    if (req.has_header("Authorization"))
    {
        auto authHeader = req.get_header_value("Authorization");
        if (authHeader.find("Basic ") == 0)
        {
            std::string credentials = authHeader.substr(6);
            if (auth->validateBasicAuth(credentials, "")) return true;
        }
    }



    return false;
}

void Custom::handleSystemhealthGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        SystemhealthGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseSystemhealthGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForSystemhealth(params);
        handleSystemhealthGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Custom::handleAppssystemconfigcomshinesolutionshealthcheckhcimplactiveBundleHealthCheckPostRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AppssystemconfigcomshinesolutionshealthcheckhcimplactiveBundleHealthCheckPostRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAppssystemconfigcomshinesolutionshealthcheckhcimplactiveBundleHealthCheckPostParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        handlePostForAppssystemconfigcomshinesolutionshealthcheckhcimplactiveBundleHealthCheck(params);
        res.status = HTTP_RESPONSE_CODE_NO_CONTENT;

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Custom::handleAppssystemconfigcomshinesolutionsaempasswordresetactivatorPostRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        AppssystemconfigcomshinesolutionsaempasswordresetactivatorPostRequest params;
        std::vector<std::string> paramErrors;
        if (!parseAppssystemconfigcomshinesolutionsaempasswordresetactivatorPostParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        handlePostForAppssystemconfigcomshinesolutionsaempasswordresetactivator(params);
        res.status = HTTP_RESPONSE_CODE_NO_CONTENT;

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}


void Custom::registerRoutes(httplib::Server& svr, std::shared_ptr<AuthenticationManager> auth)
{
    svr.Get("/system/health", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleSystemhealthGetRequest(req, res, auth);
    });
    svr.Post("/apps/system/config/com.shinesolutions.healthcheck.hc.impl.ActiveBundleHealthCheck", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAppssystemconfigcomshinesolutionshealthcheckhcimplactiveBundleHealthCheckPostRequest(req, res, auth);
    });
    svr.Post("/apps/system/config/com.shinesolutions.aem.passwordreset.Activator", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleAppssystemconfigcomshinesolutionsaempasswordresetactivatorPostRequest(req, res, auth);
    });
}

} // namespace Api