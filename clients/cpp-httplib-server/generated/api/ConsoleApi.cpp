/**
* This file is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

// System headers
#include <algorithm>
#include <cctype>
#include <locale>

// Project headers
#include "ConsoleApi.h"
#include "AuthenticationManager.h"

constexpr int HTTP_RESPONSE_CODE_PRIMITIVE_STRING = 0;
constexpr int HTTP_RESPONSE_CODE_BUNDLE_INFO = 200;
constexpr int HTTP_RESPONSE_CODE_SAML_CONFIGURATION_INFO = 200;
constexpr int HTTP_RESPONSE_CODE_NO_CONTENT = 204;
constexpr int HTTP_RESPONSE_CODE_BAD_REQUEST = 400;
constexpr int HTTP_RESPONSE_CODE_UNAUTHORIZED = 401;
constexpr int HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR = 500;

namespace Api {

using namespace models;

void Console::handleSystemconsolestatusProductinfojsonGetResponse(const SystemconsolestatusProductinfojsonGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_PRIMITIVE_STRING;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}
bool Console::parseSystemconsolebundlesnamejsonGetParams(const httplib::Request& req, Console::SystemconsolebundlesnamejsonGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Path Parameters - name (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'name'");
    }
    else
    {
        try
        {
            params.m_name = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'name': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Console::handleSystemconsolebundlesnamejsonGetResponse(const SystemconsolebundlesnamejsonGetResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::BundleInfo>)
        {
            res.status = HTTP_RESPONSE_CODE_BUNDLE_INFO;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, std::string>)
        {
            res.status = HTTP_RESPONSE_CODE_PRIMITIVE_STRING;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}
void Console::handleSystemconsoleconfigMgrGetResponse(const SystemconsoleconfigMgrGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_PRIMITIVE_STRING;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}
bool Console::parseSystemconsolebundlesnamePostParams(const httplib::Request& req, Console::SystemconsolebundlesnamePostRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - action
    if (req.has_param("action"))
    {
        params.m_action = req.get_param_value("action");
    }

    // Path Parameters - name (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'name'");
    }
    else
    {
        try
        {
            params.m_name = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'name': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
bool Console::parseSystemconsolejmxcomadobegranitetyperepositoryopactionPostParams(const httplib::Request& req, Console::SystemconsolejmxcomadobegranitetyperepositoryopactionPostRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Path Parameters - action (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'action'");
    }
    else
    {
        try
        {
            params.m_action = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'action': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
bool Console::parseSystemconsoleconfigMgrcomadobegraniteauthsamlSamlAuthenticationHandlerPostParams(const httplib::Request& req, Console::SystemconsoleconfigMgrcomadobegraniteauthsamlSamlAuthenticationHandlerPostRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - post
    if (req.has_param("post"))
    {
        try
        {
            params.m_post = (req.get_param_value("post") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'post': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_post = false;
    }
    // Query Parameters - apply
    if (req.has_param("apply"))
    {
        try
        {
            params.m_apply = (req.get_param_value("apply") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'apply': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_apply = false;
    }
    // Query Parameters - delete
    if (req.has_param("delete"))
    {
        try
        {
            params.m_r_delete = (req.get_param_value("delete") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'delete': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_r_delete = false;
    }
    // Query Parameters - action
    if (req.has_param("action"))
    {
        params.m_action = req.get_param_value("action");
    }
    else
    {
        // Use default value for optional parameter
        params.m_action = "";
    }
    // Query Parameters - $location
    if (req.has_param("$location"))
    {
        params.m_Dollarlocation = req.get_param_value("$location");
    }
    else
    {
        // Use default value for optional parameter
        params.m_Dollarlocation = "";
    }
    // Query Parameters - path
    if (req.has_param("path"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("path");
            if (!params.m_path.has_value())
            {
                params.m_path = std::vector<std::string>{};
            }
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("path", i);

                    params.m_path->emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("path", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_path->emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'path': " + std::string(e.what()));
        }
    }
    // Query Parameters - service.ranking
    if (req.has_param("service.ranking"))
    {
        try
        {
            params.m_servicePeriodranking = std::stoi(req.get_param_value("service.ranking"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'service.ranking': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_servicePeriodranking = 0;
    }
    // Query Parameters - idpUrl
    if (req.has_param("idpUrl"))
    {
        params.m_idpUrl = req.get_param_value("idpUrl");
    }
    else
    {
        // Use default value for optional parameter
        params.m_idpUrl = "";
    }
    // Query Parameters - idpCertAlias
    if (req.has_param("idpCertAlias"))
    {
        params.m_idpCertAlias = req.get_param_value("idpCertAlias");
    }
    else
    {
        // Use default value for optional parameter
        params.m_idpCertAlias = "";
    }
    // Query Parameters - idpHttpRedirect
    if (req.has_param("idpHttpRedirect"))
    {
        try
        {
            params.m_idpHttpRedirect = (req.get_param_value("idpHttpRedirect") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'idpHttpRedirect': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_idpHttpRedirect = false;
    }
    // Query Parameters - serviceProviderEntityId
    if (req.has_param("serviceProviderEntityId"))
    {
        params.m_serviceProviderEntityId = req.get_param_value("serviceProviderEntityId");
    }
    else
    {
        // Use default value for optional parameter
        params.m_serviceProviderEntityId = "";
    }
    // Query Parameters - assertionConsumerServiceURL
    if (req.has_param("assertionConsumerServiceURL"))
    {
        params.m_assertionConsumerServiceURL = req.get_param_value("assertionConsumerServiceURL");
    }
    else
    {
        // Use default value for optional parameter
        params.m_assertionConsumerServiceURL = "";
    }
    // Query Parameters - spPrivateKeyAlias
    if (req.has_param("spPrivateKeyAlias"))
    {
        params.m_spPrivateKeyAlias = req.get_param_value("spPrivateKeyAlias");
    }
    else
    {
        // Use default value for optional parameter
        params.m_spPrivateKeyAlias = "";
    }
    // Query Parameters - keyStorePassword
    if (req.has_param("keyStorePassword"))
    {
        params.m_keyStorePassword = req.get_param_value("keyStorePassword");
    }
    else
    {
        // Use default value for optional parameter
        params.m_keyStorePassword = "";
    }
    // Query Parameters - defaultRedirectUrl
    if (req.has_param("defaultRedirectUrl"))
    {
        params.m_defaultRedirectUrl = req.get_param_value("defaultRedirectUrl");
    }
    else
    {
        // Use default value for optional parameter
        params.m_defaultRedirectUrl = "";
    }
    // Query Parameters - userIDAttribute
    if (req.has_param("userIDAttribute"))
    {
        params.m_userIDAttribute = req.get_param_value("userIDAttribute");
    }
    else
    {
        // Use default value for optional parameter
        params.m_userIDAttribute = "";
    }
    // Query Parameters - useEncryption
    if (req.has_param("useEncryption"))
    {
        try
        {
            params.m_useEncryption = (req.get_param_value("useEncryption") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'useEncryption': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_useEncryption = false;
    }
    // Query Parameters - createUser
    if (req.has_param("createUser"))
    {
        try
        {
            params.m_createUser = (req.get_param_value("createUser") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'createUser': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_createUser = false;
    }
    // Query Parameters - addGroupMemberships
    if (req.has_param("addGroupMemberships"))
    {
        try
        {
            params.m_addGroupMemberships = (req.get_param_value("addGroupMemberships") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'addGroupMemberships': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_addGroupMemberships = false;
    }
    // Query Parameters - groupMembershipAttribute
    if (req.has_param("groupMembershipAttribute"))
    {
        params.m_groupMembershipAttribute = req.get_param_value("groupMembershipAttribute");
    }
    else
    {
        // Use default value for optional parameter
        params.m_groupMembershipAttribute = "";
    }
    // Query Parameters - defaultGroups
    if (req.has_param("defaultGroups"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("defaultGroups");
            if (!params.m_defaultGroups.has_value())
            {
                params.m_defaultGroups = std::vector<std::string>{};
            }
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("defaultGroups", i);

                    params.m_defaultGroups->emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("defaultGroups", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_defaultGroups->emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'defaultGroups': " + std::string(e.what()));
        }
    }
    // Query Parameters - nameIdFormat
    if (req.has_param("nameIdFormat"))
    {
        params.m_nameIdFormat = req.get_param_value("nameIdFormat");
    }
    else
    {
        // Use default value for optional parameter
        params.m_nameIdFormat = "";
    }
    // Query Parameters - synchronizeAttributes
    if (req.has_param("synchronizeAttributes"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("synchronizeAttributes");
            if (!params.m_synchronizeAttributes.has_value())
            {
                params.m_synchronizeAttributes = std::vector<std::string>{};
            }
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("synchronizeAttributes", i);

                    params.m_synchronizeAttributes->emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("synchronizeAttributes", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_synchronizeAttributes->emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'synchronizeAttributes': " + std::string(e.what()));
        }
    }
    // Query Parameters - handleLogout
    if (req.has_param("handleLogout"))
    {
        try
        {
            params.m_handleLogout = (req.get_param_value("handleLogout") == "true");
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'handleLogout': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_handleLogout = false;
    }
    // Query Parameters - logoutUrl
    if (req.has_param("logoutUrl"))
    {
        params.m_logoutUrl = req.get_param_value("logoutUrl");
    }
    else
    {
        // Use default value for optional parameter
        params.m_logoutUrl = "";
    }
    // Query Parameters - clockTolerance
    if (req.has_param("clockTolerance"))
    {
        try
        {
            params.m_clockTolerance = std::stoi(req.get_param_value("clockTolerance"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'clockTolerance': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_clockTolerance = 0;
    }
    // Query Parameters - digestMethod
    if (req.has_param("digestMethod"))
    {
        params.m_digestMethod = req.get_param_value("digestMethod");
    }
    else
    {
        // Use default value for optional parameter
        params.m_digestMethod = "";
    }
    // Query Parameters - signatureMethod
    if (req.has_param("signatureMethod"))
    {
        params.m_signatureMethod = req.get_param_value("signatureMethod");
    }
    else
    {
        // Use default value for optional parameter
        params.m_signatureMethod = "";
    }
    // Query Parameters - userIntermediatePath
    if (req.has_param("userIntermediatePath"))
    {
        params.m_userIntermediatePath = req.get_param_value("userIntermediatePath");
    }
    else
    {
        // Use default value for optional parameter
        params.m_userIntermediatePath = "";
    }
    // Query Parameters - propertylist
    if (req.has_param("propertylist"))
    {
        try
        {
            // form/simple: multi-param or comma-separated
            size_t count = req.get_param_value_count("propertylist");
            if (!params.m_propertylist.has_value())
            {
                params.m_propertylist = std::vector<std::string>{};
            }
            if (count > 1)
            {
                for (size_t i = 0; i < count; ++i)
                {
                    auto val = req.get_param_value("propertylist", i);

                    params.m_propertylist->emplace_back(val);

                }
            }
            else if (count == 1)
            {
                auto val = req.get_param_value("propertylist", 0);
                std::stringstream ss(val);
                std::string item;
                while (std::getline(ss, item, ','))
                {
                    if (!item.empty())
                    {

                        params.m_propertylist->emplace_back(item);

                    }
                }
            }
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'propertylist': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Console::handleSystemconsoleconfigMgrcomadobegraniteauthsamlSamlAuthenticationHandlerPostResponse(const SystemconsoleconfigMgrcomadobegraniteauthsamlSamlAuthenticationHandlerPostResponse& result, httplib::Response& res)
{
    std::visit([&](const auto& value)
    {
        using T = std::decay_t<decltype(value)>;

        // Success types
        if constexpr (std::is_same_v<T, models::SamlConfigurationInfo>)
        {
            res.status = HTTP_RESPONSE_CODE_SAML_CONFIGURATION_INFO;
            nlohmann::json responseJson;
            to_json(responseJson, value);
            res.set_content(responseJson.dump(), "application/json");
        }
        // Error types
        else if constexpr (std::is_same_v<T, std::string>)
        {
            res.status = HTTP_RESPONSE_CODE_PRIMITIVE_STRING;
            nlohmann::json errorJson = value;
            res.set_content(errorJson.dump(), "application/json");
        }
    }, result);
}

bool Console::performAuthentication(
    const httplib::Request& req,
    std::shared_ptr<AuthenticationManager> auth,
    httplib::Response& res)
{
    if (!auth)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "AuthenticationManager not configured";
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
        return false;
    }


    if (req.has_header("Authorization"))
    {
        auto authHeader = req.get_header_value("Authorization");
        if (authHeader.find("Basic ") == 0)
        {
            std::string credentials = authHeader.substr(6);
            if (auth->validateBasicAuth(credentials, "")) return true;
        }
    }



    return false;
}

void Console::handleSystemconsolestatusProductinfojsonGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        auto result = handleGetForSystemconsolestatusProductinfojson();
        handleSystemconsolestatusProductinfojsonGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Console::handleSystemconsolebundlesnamejsonGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        SystemconsolebundlesnamejsonGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseSystemconsolebundlesnamejsonGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForSystemconsolebundlesnamejson(params);
        handleSystemconsolebundlesnamejsonGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Console::handleSystemconsoleconfigMgrGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        auto result = handleGetForSystemconsoleconfigMgr();
        handleSystemconsoleconfigMgrGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Console::handleSystemconsolebundlesnamePostRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        SystemconsolebundlesnamePostRequest params;
        std::vector<std::string> paramErrors;
        if (!parseSystemconsolebundlesnamePostParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        handlePostForSystemconsolebundlesname(params);
        res.status = HTTP_RESPONSE_CODE_NO_CONTENT;

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Console::handleSystemconsolejmxcomadobegranitetyperepositoryopactionPostRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        SystemconsolejmxcomadobegranitetyperepositoryopactionPostRequest params;
        std::vector<std::string> paramErrors;
        if (!parseSystemconsolejmxcomadobegranitetyperepositoryopactionPostParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        handlePostForSystemconsolejmxcomadobegranitetyperepositoryopaction(params);
        res.status = HTTP_RESPONSE_CODE_NO_CONTENT;

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Console::handleSystemconsoleconfigMgrcomadobegraniteauthsamlSamlAuthenticationHandlerPostRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        SystemconsoleconfigMgrcomadobegraniteauthsamlSamlAuthenticationHandlerPostRequest params;
        std::vector<std::string> paramErrors;
        if (!parseSystemconsoleconfigMgrcomadobegraniteauthsamlSamlAuthenticationHandlerPostParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handlePostForSystemconsoleconfigMgrcomadobegraniteauthsamlSamlAuthenticationHandler(params);
        handleSystemconsoleconfigMgrcomadobegraniteauthsamlSamlAuthenticationHandlerPostResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}


void Console::registerRoutes(httplib::Server& svr, std::shared_ptr<AuthenticationManager> auth)
{
    svr.Get("/system/console/status-productinfo.json", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleSystemconsolestatusProductinfojsonGetRequest(req, res, auth);
    });
    svr.Get("/system/console/bundles/{name}.json", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleSystemconsolebundlesnamejsonGetRequest(req, res, auth);
    });
    svr.Get("/system/console/configMgr", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleSystemconsoleconfigMgrGetRequest(req, res, auth);
    });
    svr.Post("/system/console/bundles/{name}", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleSystemconsolebundlesnamePostRequest(req, res, auth);
    });
    svr.Post("/system/console/jmx/com.adobe.granite:type&#x3D;Repository/op/{action}", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleSystemconsolejmxcomadobegranitetyperepositoryopactionPostRequest(req, res, auth);
    });
    svr.Post("/system/console/configMgr/com.adobe.granite.auth.saml.SamlAuthenticationHandler", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleSystemconsoleconfigMgrcomadobegraniteauthsamlSamlAuthenticationHandlerPostRequest(req, res, auth);
    });
}

} // namespace Api