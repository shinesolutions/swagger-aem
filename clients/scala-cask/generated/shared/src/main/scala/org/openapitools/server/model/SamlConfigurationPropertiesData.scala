/**
 * Adobe Experience Manager (AEM) API
 * Swagger AEM is an OpenAPI specification for Adobe Experience Manager (AEM) API
 *
 * OpenAPI spec version: 3.7.1-pre.0
 *
 * Contact: opensource@shinesolutions.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** SamlConfigurationPropertiesData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class SamlConfigurationPropertiesData(
  path: SamlConfigurationPropertyItemsArrayData = null ,
  serviceRanking: SamlConfigurationPropertyItemsLongData = null ,
  idpUrl: SamlConfigurationPropertyItemsStringData = null ,
  idpCertAlias: SamlConfigurationPropertyItemsStringData = null ,
  idpHttpRedirect: SamlConfigurationPropertyItemsBooleanData = null ,
  serviceProviderEntityId: SamlConfigurationPropertyItemsStringData = null ,
  assertionConsumerServiceURL: SamlConfigurationPropertyItemsStringData = null ,
  spPrivateKeyAlias: SamlConfigurationPropertyItemsStringData = null ,
  keyStorePassword: SamlConfigurationPropertyItemsStringData = null ,
  defaultRedirectUrl: SamlConfigurationPropertyItemsStringData = null ,
  userIDAttribute: SamlConfigurationPropertyItemsStringData = null ,
  useEncryption: SamlConfigurationPropertyItemsBooleanData = null ,
  createUser: SamlConfigurationPropertyItemsBooleanData = null ,
  addGroupMemberships: SamlConfigurationPropertyItemsBooleanData = null ,
  groupMembershipAttribute: SamlConfigurationPropertyItemsStringData = null ,
  defaultGroups: SamlConfigurationPropertyItemsArrayData = null ,
  nameIdFormat: SamlConfigurationPropertyItemsStringData = null ,
  synchronizeAttributes: SamlConfigurationPropertyItemsArrayData = null ,
  handleLogout: SamlConfigurationPropertyItemsBooleanData = null ,
  logoutUrl: SamlConfigurationPropertyItemsStringData = null ,
  clockTolerance: SamlConfigurationPropertyItemsLongData = null ,
  digestMethod: SamlConfigurationPropertyItemsStringData = null ,
  signatureMethod: SamlConfigurationPropertyItemsStringData = null ,
  userIntermediatePath: SamlConfigurationPropertyItemsStringData = null 
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== path validation ==================
        
        
        
        
        // validating path
        if (_allValidationErrors.isEmpty || !failFast) {
            if path != null then _allValidationErrors ++= path.validationErrors(path :+ SamlConfigurationProperties.Fields.path, failFast)
        }

        // ================== serviceRanking validation ==================
        
        
        
        
        // validating serviceRanking
        if (_allValidationErrors.isEmpty || !failFast) {
            if serviceRanking != null then _allValidationErrors ++= serviceRanking.validationErrors(path :+ SamlConfigurationProperties.Fields.serviceRanking, failFast)
        }

        // ================== idpUrl validation ==================
        
        
        
        
        // validating idpUrl
        if (_allValidationErrors.isEmpty || !failFast) {
            if idpUrl != null then _allValidationErrors ++= idpUrl.validationErrors(path :+ SamlConfigurationProperties.Fields.idpUrl, failFast)
        }

        // ================== idpCertAlias validation ==================
        
        
        
        
        // validating idpCertAlias
        if (_allValidationErrors.isEmpty || !failFast) {
            if idpCertAlias != null then _allValidationErrors ++= idpCertAlias.validationErrors(path :+ SamlConfigurationProperties.Fields.idpCertAlias, failFast)
        }

        // ================== idpHttpRedirect validation ==================
        
        
        
        
        // validating idpHttpRedirect
        if (_allValidationErrors.isEmpty || !failFast) {
            if idpHttpRedirect != null then _allValidationErrors ++= idpHttpRedirect.validationErrors(path :+ SamlConfigurationProperties.Fields.idpHttpRedirect, failFast)
        }

        // ================== serviceProviderEntityId validation ==================
        
        
        
        
        // validating serviceProviderEntityId
        if (_allValidationErrors.isEmpty || !failFast) {
            if serviceProviderEntityId != null then _allValidationErrors ++= serviceProviderEntityId.validationErrors(path :+ SamlConfigurationProperties.Fields.serviceProviderEntityId, failFast)
        }

        // ================== assertionConsumerServiceURL validation ==================
        
        
        
        
        // validating assertionConsumerServiceURL
        if (_allValidationErrors.isEmpty || !failFast) {
            if assertionConsumerServiceURL != null then _allValidationErrors ++= assertionConsumerServiceURL.validationErrors(path :+ SamlConfigurationProperties.Fields.assertionConsumerServiceURL, failFast)
        }

        // ================== spPrivateKeyAlias validation ==================
        
        
        
        
        // validating spPrivateKeyAlias
        if (_allValidationErrors.isEmpty || !failFast) {
            if spPrivateKeyAlias != null then _allValidationErrors ++= spPrivateKeyAlias.validationErrors(path :+ SamlConfigurationProperties.Fields.spPrivateKeyAlias, failFast)
        }

        // ================== keyStorePassword validation ==================
        
        
        
        
        // validating keyStorePassword
        if (_allValidationErrors.isEmpty || !failFast) {
            if keyStorePassword != null then _allValidationErrors ++= keyStorePassword.validationErrors(path :+ SamlConfigurationProperties.Fields.keyStorePassword, failFast)
        }

        // ================== defaultRedirectUrl validation ==================
        
        
        
        
        // validating defaultRedirectUrl
        if (_allValidationErrors.isEmpty || !failFast) {
            if defaultRedirectUrl != null then _allValidationErrors ++= defaultRedirectUrl.validationErrors(path :+ SamlConfigurationProperties.Fields.defaultRedirectUrl, failFast)
        }

        // ================== userIDAttribute validation ==================
        
        
        
        
        // validating userIDAttribute
        if (_allValidationErrors.isEmpty || !failFast) {
            if userIDAttribute != null then _allValidationErrors ++= userIDAttribute.validationErrors(path :+ SamlConfigurationProperties.Fields.userIDAttribute, failFast)
        }

        // ================== useEncryption validation ==================
        
        
        
        
        // validating useEncryption
        if (_allValidationErrors.isEmpty || !failFast) {
            if useEncryption != null then _allValidationErrors ++= useEncryption.validationErrors(path :+ SamlConfigurationProperties.Fields.useEncryption, failFast)
        }

        // ================== createUser validation ==================
        
        
        
        
        // validating createUser
        if (_allValidationErrors.isEmpty || !failFast) {
            if createUser != null then _allValidationErrors ++= createUser.validationErrors(path :+ SamlConfigurationProperties.Fields.createUser, failFast)
        }

        // ================== addGroupMemberships validation ==================
        
        
        
        
        // validating addGroupMemberships
        if (_allValidationErrors.isEmpty || !failFast) {
            if addGroupMemberships != null then _allValidationErrors ++= addGroupMemberships.validationErrors(path :+ SamlConfigurationProperties.Fields.addGroupMemberships, failFast)
        }

        // ================== groupMembershipAttribute validation ==================
        
        
        
        
        // validating groupMembershipAttribute
        if (_allValidationErrors.isEmpty || !failFast) {
            if groupMembershipAttribute != null then _allValidationErrors ++= groupMembershipAttribute.validationErrors(path :+ SamlConfigurationProperties.Fields.groupMembershipAttribute, failFast)
        }

        // ================== defaultGroups validation ==================
        
        
        
        
        // validating defaultGroups
        if (_allValidationErrors.isEmpty || !failFast) {
            if defaultGroups != null then _allValidationErrors ++= defaultGroups.validationErrors(path :+ SamlConfigurationProperties.Fields.defaultGroups, failFast)
        }

        // ================== nameIdFormat validation ==================
        
        
        
        
        // validating nameIdFormat
        if (_allValidationErrors.isEmpty || !failFast) {
            if nameIdFormat != null then _allValidationErrors ++= nameIdFormat.validationErrors(path :+ SamlConfigurationProperties.Fields.nameIdFormat, failFast)
        }

        // ================== synchronizeAttributes validation ==================
        
        
        
        
        // validating synchronizeAttributes
        if (_allValidationErrors.isEmpty || !failFast) {
            if synchronizeAttributes != null then _allValidationErrors ++= synchronizeAttributes.validationErrors(path :+ SamlConfigurationProperties.Fields.synchronizeAttributes, failFast)
        }

        // ================== handleLogout validation ==================
        
        
        
        
        // validating handleLogout
        if (_allValidationErrors.isEmpty || !failFast) {
            if handleLogout != null then _allValidationErrors ++= handleLogout.validationErrors(path :+ SamlConfigurationProperties.Fields.handleLogout, failFast)
        }

        // ================== logoutUrl validation ==================
        
        
        
        
        // validating logoutUrl
        if (_allValidationErrors.isEmpty || !failFast) {
            if logoutUrl != null then _allValidationErrors ++= logoutUrl.validationErrors(path :+ SamlConfigurationProperties.Fields.logoutUrl, failFast)
        }

        // ================== clockTolerance validation ==================
        
        
        
        
        // validating clockTolerance
        if (_allValidationErrors.isEmpty || !failFast) {
            if clockTolerance != null then _allValidationErrors ++= clockTolerance.validationErrors(path :+ SamlConfigurationProperties.Fields.clockTolerance, failFast)
        }

        // ================== digestMethod validation ==================
        
        
        
        
        // validating digestMethod
        if (_allValidationErrors.isEmpty || !failFast) {
            if digestMethod != null then _allValidationErrors ++= digestMethod.validationErrors(path :+ SamlConfigurationProperties.Fields.digestMethod, failFast)
        }

        // ================== signatureMethod validation ==================
        
        
        
        
        // validating signatureMethod
        if (_allValidationErrors.isEmpty || !failFast) {
            if signatureMethod != null then _allValidationErrors ++= signatureMethod.validationErrors(path :+ SamlConfigurationProperties.Fields.signatureMethod, failFast)
        }

        // ================== userIntermediatePath validation ==================
        
        
        
        
        // validating userIntermediatePath
        if (_allValidationErrors.isEmpty || !failFast) {
            if userIntermediatePath != null then _allValidationErrors ++= userIntermediatePath.validationErrors(path :+ SamlConfigurationProperties.Fields.userIntermediatePath, failFast)
        }

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[SamlConfigurationProperties] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : SamlConfigurationProperties = {
    SamlConfigurationProperties(
        path = Option(path).map(_.asModel) /* 4 */,
        serviceRanking = Option(serviceRanking).map(_.asModel) /* 4 */,
        idpUrl = Option(idpUrl).map(_.asModel) /* 4 */,
        idpCertAlias = Option(idpCertAlias).map(_.asModel) /* 4 */,
        idpHttpRedirect = Option(idpHttpRedirect).map(_.asModel) /* 4 */,
        serviceProviderEntityId = Option(serviceProviderEntityId).map(_.asModel) /* 4 */,
        assertionConsumerServiceURL = Option(assertionConsumerServiceURL).map(_.asModel) /* 4 */,
        spPrivateKeyAlias = Option(spPrivateKeyAlias).map(_.asModel) /* 4 */,
        keyStorePassword = Option(keyStorePassword).map(_.asModel) /* 4 */,
        defaultRedirectUrl = Option(defaultRedirectUrl).map(_.asModel) /* 4 */,
        userIDAttribute = Option(userIDAttribute).map(_.asModel) /* 4 */,
        useEncryption = Option(useEncryption).map(_.asModel) /* 4 */,
        createUser = Option(createUser).map(_.asModel) /* 4 */,
        addGroupMemberships = Option(addGroupMemberships).map(_.asModel) /* 4 */,
        groupMembershipAttribute = Option(groupMembershipAttribute).map(_.asModel) /* 4 */,
        defaultGroups = Option(defaultGroups).map(_.asModel) /* 4 */,
        nameIdFormat = Option(nameIdFormat).map(_.asModel) /* 4 */,
        synchronizeAttributes = Option(synchronizeAttributes).map(_.asModel) /* 4 */,
        handleLogout = Option(handleLogout).map(_.asModel) /* 4 */,
        logoutUrl = Option(logoutUrl).map(_.asModel) /* 4 */,
        clockTolerance = Option(clockTolerance).map(_.asModel) /* 4 */,
        digestMethod = Option(digestMethod).map(_.asModel) /* 4 */,
        signatureMethod = Option(signatureMethod).map(_.asModel) /* 4 */,
        userIntermediatePath = Option(userIntermediatePath).map(_.asModel) /* 4 */
    
    )
  }
}

object SamlConfigurationPropertiesData {

  def validated(d8a : SamlConfigurationPropertiesData, failFast : Boolean) : scala.util.Try[SamlConfigurationProperties] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : SamlConfigurationPropertiesData = try {
        val data = read[SamlConfigurationPropertiesData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating SamlConfigurationPropertiesData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : SamlConfigurationPropertiesData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[SamlConfigurationPropertiesData] = try {
        read[List[SamlConfigurationPropertiesData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[SamlConfigurationProperties]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[SamlConfigurationProperties]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, SamlConfigurationPropertiesData] = try {
        read[Map[String, SamlConfigurationPropertiesData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, SamlConfigurationProperties]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, SamlConfigurationProperties]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

