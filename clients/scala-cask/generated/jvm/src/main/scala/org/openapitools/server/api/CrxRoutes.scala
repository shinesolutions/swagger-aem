//> using scala "3.3.1"
//> using lib "com.lihaoyi::cask:0.8.3"
//> using lib "com.lihaoyi::scalatags:0.12.0"
/**
 * Adobe Experience Manager (AEM) API
 * Swagger AEM is an OpenAPI specification for Adobe Experience Manager (AEM) API
 *
 * OpenAPI spec version: 3.7.1-pre.0
 *
 * Contact: opensource@shinesolutions.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */


// this is generated from apiRoutes.mustache
package org.openapitools.server.api

import org.openapitools.server.model.*

import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*
import scala.util.Try

import java.io.File
import org.openapitools.server.model.InstallStatus

class CrxRoutes(service : CrxService[Try]) extends cask.Routes {


        /** 
         * 
         */
        @cask.get("/crx/server/crx.default/jcr:root/.1.json")
        def getCrxdeStatus(request: cask.Request) = {
            // auth method aemAuth : http, keyParamName: 

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.getCrxdeStatus())
            result <- Parsed.fromTry(resultTry)
        } yield result


        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : String) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(value : String) => cask.Response(data = write(value), 404, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** 
         * 
         */
        @cask.get("/crx/packmgr/installstatus.jsp")
        def getInstallStatus(request: cask.Request) = {
            // auth method aemAuth : http, keyParamName: 

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.getInstallStatus())
            result <- Parsed.fromTry(resultTry)
        } yield result

        import InstallStatus | String.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : InstallStatus) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(value : String) => cask.Response(data = write(value), 0, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** 
         * 
         */
        @cask.get("/crx/packmgr/service/script.html")
        def getPackageManagerServlet(request: cask.Request) = {
            // auth method aemAuth : http, keyParamName: 

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.getPackageManagerServlet())
            result <- Parsed.fromTry(resultTry)
        } yield result


        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : String) => cask.Response(data = write(value), 404, headers = Seq("Content-Type" -> "application/json"))
          case Right(value : String) => cask.Response(data = write(value), 405, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** 
         * 
         */
        @cask.post("/crx/packmgr/service.jsp")
        def postPackageService(request: cask.Request, cmd : String) = {
            // auth method aemAuth : http, keyParamName: 

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.postPackageService(cmd))
            result <- Parsed.fromTry(resultTry)
        } yield result


        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : String) => cask.Response(data = write(value), 0, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** 
         * 
         */
        @cask.post("/crx/packmgr/service/.json/:path")
        def postPackageServiceJson(path : String, request: cask.Request, cmd : String, groupName : Option[String], packageName : Option[String], packageVersion : Option[String], charset : Option[String], force : Option[Boolean], recursive : Option[Boolean]) = {
            // auth method aemAuth : http, keyParamName: 

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            path <- Parsed(path)
        `package` <- request.formValueAsFileOptional("`package`")
            resultTry <- Parsed.eval(service.postPackageServiceJson(path, cmd, groupName, packageName, packageVersion, charset, force, recursive, `package`))
            result <- Parsed.fromTry(resultTry)
        } yield result


        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : String) => cask.Response(data = write(value), 0, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** 
         * 
         */
        @cask.post("/crx/packmgr/update.jsp")
        def postPackageUpdate(request: cask.Request, groupName : String, packageName : String, version : String, path : String, filter : Option[String], charset : Option[String]) = {
            // auth method aemAuth : http, keyParamName: 

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.postPackageUpdate(groupName, packageName, version, path, filter, charset))
            result <- Parsed.fromTry(resultTry)
        } yield result


        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : String) => cask.Response(data = write(value), 0, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** 
         * 
         */
        @cask.post("/crx/explorer/ui/setpassword.jsp")
        def postSetPassword(request: cask.Request, old : String, plain : String, verify : String) = {
            // auth method aemAuth : http, keyParamName: 

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.postSetPassword(old, plain, verify))
            result <- Parsed.fromTry(resultTry)
        } yield result


        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : String) => cask.Response(data = write(value), 0, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }

    initialize()
}
