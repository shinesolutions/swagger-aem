#' Adobe Experience Manager (AEM) API
#'
#' Swagger AEM is an OpenAPI specification for Adobe Experience Manager (AEM) API
#'
#' The version of the OpenAPI document: 3.7.1-pre.0
#' Contact: opensource@shinesolutions.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Custom operations
#' @description CustomApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  GetAemHealthCheck  ####################
#'
#' library(openapi)
#' var_tags <- "tags_example" # character |  (Optional)
#' var_combine_tags_or <- "combine_tags_or_example" # character |  (Optional)
#'
#' api_instance <- CustomApi$new()
#'
#' # Configure HTTP basic authorization: aemAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAemHealthCheck(tags = var_tags, combine_tags_or = var_combine_tags_ordata_file = "result.txt")
#' result <- api_instance$GetAemHealthCheck(tags = var_tags, combine_tags_or = var_combine_tags_or)
#' dput(result)
#'
#'
#' ####################  PostConfigAemHealthCheckServlet  ####################
#'
#' library(openapi)
#' var_bundles_ignored <- c("inner_example") # array[character] |  (Optional)
#' var_bundles_ignored_type_hint <- "bundles_ignored_type_hint_example" # character |  (Optional)
#'
#' api_instance <- CustomApi$new()
#'
#' # Configure HTTP basic authorization: aemAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' api_instance$PostConfigAemHealthCheckServlet(bundles_ignored = var_bundles_ignored, bundles_ignored_type_hint = var_bundles_ignored_type_hint)
#'
#'
#' ####################  PostConfigAemPasswordReset  ####################
#'
#' library(openapi)
#' var_pwdreset_authorizables <- c("inner_example") # array[character] |  (Optional)
#' var_pwdreset_authorizables_type_hint <- "pwdreset_authorizables_type_hint_example" # character |  (Optional)
#'
#' api_instance <- CustomApi$new()
#'
#' # Configure HTTP basic authorization: aemAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' api_instance$PostConfigAemPasswordReset(pwdreset_authorizables = var_pwdreset_authorizables, pwdreset_authorizables_type_hint = var_pwdreset_authorizables_type_hint)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
CustomApi <- R6::R6Class(
  "CustomApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new CustomApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' 
    #'
    #' @param tags (optional) No description
    #' @param combine_tags_or (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return character
    GetAemHealthCheck = function(tags = NULL, combine_tags_or = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetAemHealthCheckWithHttpInfo(tags, combine_tags_or, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param tags (optional) No description
    #' @param combine_tags_or (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (character) with additional information such as HTTP status code, headers
    GetAemHealthCheckWithHttpInfo = function(tags = NULL, combine_tags_or = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`tags`) && is.null(`tags`)) {
        stop("Invalid value for `tags` when calling CustomApi$GetAemHealthCheck, `tags` is not nullable")
      }

      if (!missing(`combine_tags_or`) && is.null(`combine_tags_or`)) {
        stop("Invalid value for `combine_tags_or` when calling CustomApi$GetAemHealthCheck, `combine_tags_or` is not nullable")
      }

      query_params[["tags"]] <- `tags`

      query_params[["combineTagsOr"]] <- `combine_tags_or`

      local_var_url_path <- "/system/health"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "character"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param bundles_ignored (optional) No description
    #' @param bundles_ignored_type_hint (optional) No description
    #' @param ... Other optional arguments
    #'
    #' @return void
    PostConfigAemHealthCheckServlet = function(bundles_ignored = NULL, bundles_ignored_type_hint = NULL, ...) {
      local_var_response <- self$PostConfigAemHealthCheckServletWithHttpInfo(bundles_ignored, bundles_ignored_type_hint, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param bundles_ignored (optional) No description
    #' @param bundles_ignored_type_hint (optional) No description
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    PostConfigAemHealthCheckServletWithHttpInfo = function(bundles_ignored = NULL, bundles_ignored_type_hint = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`bundles_ignored`) && is.null(`bundles_ignored`)) {
        stop("Invalid value for `bundles_ignored` when calling CustomApi$PostConfigAemHealthCheckServlet, `bundles_ignored` is not nullable")
      }

      if (!missing(`bundles_ignored_type_hint`) && is.null(`bundles_ignored_type_hint`)) {
        stop("Invalid value for `bundles_ignored_type_hint` when calling CustomApi$PostConfigAemHealthCheckServlet, `bundles_ignored_type_hint` is not nullable")
      }

      # explore
      for (query_item in `bundles_ignored`) {
        query_params[["bundles.ignored"]] <- c(query_params[["bundles.ignored"]], list(`bundles.ignored` = query_item))
      }

      query_params[["bundles.ignored@TypeHint"]] <- `bundles_ignored_type_hint`

      local_var_url_path <- "/apps/system/config/com.shinesolutions.healthcheck.hc.impl.ActiveBundleHealthCheck"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param pwdreset_authorizables (optional) No description
    #' @param pwdreset_authorizables_type_hint (optional) No description
    #' @param ... Other optional arguments
    #'
    #' @return void
    PostConfigAemPasswordReset = function(pwdreset_authorizables = NULL, pwdreset_authorizables_type_hint = NULL, ...) {
      local_var_response <- self$PostConfigAemPasswordResetWithHttpInfo(pwdreset_authorizables, pwdreset_authorizables_type_hint, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param pwdreset_authorizables (optional) No description
    #' @param pwdreset_authorizables_type_hint (optional) No description
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    PostConfigAemPasswordResetWithHttpInfo = function(pwdreset_authorizables = NULL, pwdreset_authorizables_type_hint = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (!missing(`pwdreset_authorizables`) && is.null(`pwdreset_authorizables`)) {
        stop("Invalid value for `pwdreset_authorizables` when calling CustomApi$PostConfigAemPasswordReset, `pwdreset_authorizables` is not nullable")
      }

      if (!missing(`pwdreset_authorizables_type_hint`) && is.null(`pwdreset_authorizables_type_hint`)) {
        stop("Invalid value for `pwdreset_authorizables_type_hint` when calling CustomApi$PostConfigAemPasswordReset, `pwdreset_authorizables_type_hint` is not nullable")
      }

      # explore
      for (query_item in `pwdreset_authorizables`) {
        query_params[["pwdreset.authorizables"]] <- c(query_params[["pwdreset.authorizables"]], list(`pwdreset.authorizables` = query_item))
      }

      query_params[["pwdreset.authorizables@TypeHint"]] <- `pwdreset_authorizables_type_hint`

      local_var_url_path <- "/apps/system/config/com.shinesolutions.aem.passwordreset.Activator"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
