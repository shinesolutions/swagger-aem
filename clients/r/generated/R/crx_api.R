#' Adobe Experience Manager (AEM) API
#'
#' Swagger AEM is an OpenAPI specification for Adobe Experience Manager (AEM) API
#'
#' The version of the OpenAPI document: 3.7.1-pre.0
#' Contact: opensource@shinesolutions.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Crx operations
#' @description CrxApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  GetCrxdeStatus  ####################
#'
#' library(openapi)
#'
#' api_instance <- CrxApi$new()
#'
#' # Configure HTTP basic authorization: aemAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCrxdeStatus(data_file = "result.txt")
#' result <- api_instance$GetCrxdeStatus()
#' dput(result)
#'
#'
#' ####################  GetInstallStatus  ####################
#'
#' library(openapi)
#'
#' api_instance <- CrxApi$new()
#'
#' # Configure HTTP basic authorization: aemAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetInstallStatus(data_file = "result.txt")
#' result <- api_instance$GetInstallStatus()
#' dput(result)
#'
#'
#' ####################  GetPackageManagerServlet  ####################
#'
#' library(openapi)
#'
#' api_instance <- CrxApi$new()
#'
#' # Configure HTTP basic authorization: aemAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' api_instance$GetPackageManagerServlet()
#'
#'
#' ####################  PostPackageService  ####################
#'
#' library(openapi)
#' var_cmd <- "cmd_example" # character | 
#'
#' api_instance <- CrxApi$new()
#'
#' # Configure HTTP basic authorization: aemAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostPackageService(var_cmddata_file = "result.txt")
#' result <- api_instance$PostPackageService(var_cmd)
#' dput(result)
#'
#'
#' ####################  PostPackageServiceJson  ####################
#'
#' library(openapi)
#' var_path <- "path_example" # character | 
#' var_cmd <- "cmd_example" # character | 
#' var_group_name <- "group_name_example" # character |  (Optional)
#' var_package_name <- "package_name_example" # character |  (Optional)
#' var_package_version <- "package_version_example" # character |  (Optional)
#' var__charset_ <- "_charset__example" # character |  (Optional)
#' var_force <- "force_example" # character |  (Optional)
#' var_recursive <- "recursive_example" # character |  (Optional)
#' var_package <- File.new('/path/to/file') # data.frame |  (Optional)
#'
#' api_instance <- CrxApi$new()
#'
#' # Configure HTTP basic authorization: aemAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostPackageServiceJson(var_path, var_cmd, group_name = var_group_name, package_name = var_package_name, package_version = var_package_version, _charset_ = var__charset_, force = var_force, recursive = var_recursive, package = var_packagedata_file = "result.txt")
#' result <- api_instance$PostPackageServiceJson(var_path, var_cmd, group_name = var_group_name, package_name = var_package_name, package_version = var_package_version, _charset_ = var__charset_, force = var_force, recursive = var_recursive, package = var_package)
#' dput(result)
#'
#'
#' ####################  PostPackageUpdate  ####################
#'
#' library(openapi)
#' var_group_name <- "group_name_example" # character | 
#' var_package_name <- "package_name_example" # character | 
#' var_version <- "version_example" # character | 
#' var_path <- "path_example" # character | 
#' var_filter <- "filter_example" # character |  (Optional)
#' var__charset_ <- "_charset__example" # character |  (Optional)
#'
#' api_instance <- CrxApi$new()
#'
#' # Configure HTTP basic authorization: aemAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostPackageUpdate(var_group_name, var_package_name, var_version, var_path, filter = var_filter, _charset_ = var__charset_data_file = "result.txt")
#' result <- api_instance$PostPackageUpdate(var_group_name, var_package_name, var_version, var_path, filter = var_filter, _charset_ = var__charset_)
#' dput(result)
#'
#'
#' ####################  PostSetPassword  ####################
#'
#' library(openapi)
#' var_old <- "old_example" # character | 
#' var_plain <- "plain_example" # character | 
#' var_verify <- "verify_example" # character | 
#'
#' api_instance <- CrxApi$new()
#'
#' # Configure HTTP basic authorization: aemAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostSetPassword(var_old, var_plain, var_verifydata_file = "result.txt")
#' result <- api_instance$PostSetPassword(var_old, var_plain, var_verify)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
CrxApi <- R6::R6Class(
  "CrxApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new CrxApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return character
    GetCrxdeStatus = function(data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetCrxdeStatusWithHttpInfo(data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (character) with additional information such as HTTP status code, headers
    GetCrxdeStatusWithHttpInfo = function(data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/crx/server/crx.default/jcr:root/.1.json"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("plain/text")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "character"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return InstallStatus
    GetInstallStatus = function(data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetInstallStatusWithHttpInfo(data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (InstallStatus) with additional information such as HTTP status code, headers
    GetInstallStatusWithHttpInfo = function(data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/crx/packmgr/installstatus.jsp"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "InstallStatus"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param ... Other optional arguments
    #'
    #' @return void
    GetPackageManagerServlet = function(...) {
      local_var_response <- self$GetPackageManagerServletWithHttpInfo(...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    GetPackageManagerServletWithHttpInfo = function(...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/crx/packmgr/service/script.html"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/html")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param cmd 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return character
    PostPackageService = function(cmd, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$PostPackageServiceWithHttpInfo(cmd, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param cmd 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (character) with additional information such as HTTP status code, headers
    PostPackageServiceWithHttpInfo = function(cmd, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`cmd`)) {
        stop("Missing required parameter `cmd`.")
      }

      if (!missing(`cmd`) && is.null(`cmd`)) {
        stop("Invalid value for `cmd` when calling CrxApi$PostPackageService, `cmd` is not nullable")
      }

      query_params[["cmd"]] <- `cmd`

      local_var_url_path <- "/crx/packmgr/service.jsp"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/xml")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "character"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param path 
    #' @param cmd 
    #' @param group_name (optional) No description
    #' @param package_name (optional) No description
    #' @param package_version (optional) No description
    #' @param _charset_ (optional) No description
    #' @param force (optional) No description
    #' @param recursive (optional) No description
    #' @param package (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return character
    PostPackageServiceJson = function(path, cmd, group_name = NULL, package_name = NULL, package_version = NULL, _charset_ = NULL, force = NULL, recursive = NULL, package = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$PostPackageServiceJsonWithHttpInfo(path, cmd, group_name, package_name, package_version, _charset_, force, recursive, package, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param path 
    #' @param cmd 
    #' @param group_name (optional) No description
    #' @param package_name (optional) No description
    #' @param package_version (optional) No description
    #' @param _charset_ (optional) No description
    #' @param force (optional) No description
    #' @param recursive (optional) No description
    #' @param package (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (character) with additional information such as HTTP status code, headers
    PostPackageServiceJsonWithHttpInfo = function(path, cmd, group_name = NULL, package_name = NULL, package_version = NULL, _charset_ = NULL, force = NULL, recursive = NULL, package = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`path`)) {
        stop("Missing required parameter `path`.")
      }

      if (missing(`cmd`)) {
        stop("Missing required parameter `cmd`.")
      }

      if (!missing(`path`) && is.null(`path`)) {
        stop("Invalid value for `path` when calling CrxApi$PostPackageServiceJson, `path` is not nullable")
      }

      if (!missing(`cmd`) && is.null(`cmd`)) {
        stop("Invalid value for `cmd` when calling CrxApi$PostPackageServiceJson, `cmd` is not nullable")
      }

      if (!missing(`group_name`) && is.null(`group_name`)) {
        stop("Invalid value for `group_name` when calling CrxApi$PostPackageServiceJson, `group_name` is not nullable")
      }

      if (!missing(`package_name`) && is.null(`package_name`)) {
        stop("Invalid value for `package_name` when calling CrxApi$PostPackageServiceJson, `package_name` is not nullable")
      }

      if (!missing(`package_version`) && is.null(`package_version`)) {
        stop("Invalid value for `package_version` when calling CrxApi$PostPackageServiceJson, `package_version` is not nullable")
      }

      if (!missing(`_charset_`) && is.null(`_charset_`)) {
        stop("Invalid value for `_charset_` when calling CrxApi$PostPackageServiceJson, `_charset_` is not nullable")
      }

      if (!missing(`force`) && is.null(`force`)) {
        stop("Invalid value for `force` when calling CrxApi$PostPackageServiceJson, `force` is not nullable")
      }

      if (!missing(`recursive`) && is.null(`recursive`)) {
        stop("Invalid value for `recursive` when calling CrxApi$PostPackageServiceJson, `recursive` is not nullable")
      }

      if (!missing(`package`) && is.null(`package`)) {
        stop("Invalid value for `package` when calling CrxApi$PostPackageServiceJson, `package` is not nullable")
      }

      query_params[["cmd"]] <- `cmd`

      query_params[["groupName"]] <- `group_name`

      query_params[["packageName"]] <- `package_name`

      query_params[["packageVersion"]] <- `package_version`

      query_params[["_charset_"]] <- `_charset_`

      query_params[["force"]] <- `force`

      query_params[["recursive"]] <- `recursive`

      file_params["package"] <- httr::upload_file(`package`)
      local_var_url_path <- "/crx/packmgr/service/.json/{path}"
      if (!missing(`path`)) {
        local_var_url_path <- gsub("\\{path\\}", URLencode(as.character(`path`), reserved = TRUE), local_var_url_path)
      }

      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("multipart/form-data")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "character"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param group_name 
    #' @param package_name 
    #' @param version 
    #' @param path 
    #' @param filter (optional) No description
    #' @param _charset_ (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return character
    PostPackageUpdate = function(group_name, package_name, version, path, filter = NULL, _charset_ = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$PostPackageUpdateWithHttpInfo(group_name, package_name, version, path, filter, _charset_, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param group_name 
    #' @param package_name 
    #' @param version 
    #' @param path 
    #' @param filter (optional) No description
    #' @param _charset_ (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (character) with additional information such as HTTP status code, headers
    PostPackageUpdateWithHttpInfo = function(group_name, package_name, version, path, filter = NULL, _charset_ = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`group_name`)) {
        stop("Missing required parameter `group_name`.")
      }

      if (missing(`package_name`)) {
        stop("Missing required parameter `package_name`.")
      }

      if (missing(`version`)) {
        stop("Missing required parameter `version`.")
      }

      if (missing(`path`)) {
        stop("Missing required parameter `path`.")
      }

      if (!missing(`group_name`) && is.null(`group_name`)) {
        stop("Invalid value for `group_name` when calling CrxApi$PostPackageUpdate, `group_name` is not nullable")
      }

      if (!missing(`package_name`) && is.null(`package_name`)) {
        stop("Invalid value for `package_name` when calling CrxApi$PostPackageUpdate, `package_name` is not nullable")
      }

      if (!missing(`version`) && is.null(`version`)) {
        stop("Invalid value for `version` when calling CrxApi$PostPackageUpdate, `version` is not nullable")
      }

      if (!missing(`path`) && is.null(`path`)) {
        stop("Invalid value for `path` when calling CrxApi$PostPackageUpdate, `path` is not nullable")
      }

      if (!missing(`filter`) && is.null(`filter`)) {
        stop("Invalid value for `filter` when calling CrxApi$PostPackageUpdate, `filter` is not nullable")
      }

      if (!missing(`_charset_`) && is.null(`_charset_`)) {
        stop("Invalid value for `_charset_` when calling CrxApi$PostPackageUpdate, `_charset_` is not nullable")
      }

      query_params[["groupName"]] <- `group_name`

      query_params[["packageName"]] <- `package_name`

      query_params[["version"]] <- `version`

      query_params[["path"]] <- `path`

      query_params[["filter"]] <- `filter`

      query_params[["_charset_"]] <- `_charset_`

      local_var_url_path <- "/crx/packmgr/update.jsp"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "character"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' 
    #'
    #' @param old 
    #' @param plain 
    #' @param verify 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return character
    PostSetPassword = function(old, plain, verify, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$PostSetPasswordWithHttpInfo(old, plain, verify, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param old 
    #' @param plain 
    #' @param verify 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (character) with additional information such as HTTP status code, headers
    PostSetPasswordWithHttpInfo = function(old, plain, verify, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`old`)) {
        stop("Missing required parameter `old`.")
      }

      if (missing(`plain`)) {
        stop("Missing required parameter `plain`.")
      }

      if (missing(`verify`)) {
        stop("Missing required parameter `verify`.")
      }

      if (!missing(`old`) && is.null(`old`)) {
        stop("Invalid value for `old` when calling CrxApi$PostSetPassword, `old` is not nullable")
      }

      if (!missing(`plain`) && is.null(`plain`)) {
        stop("Invalid value for `plain` when calling CrxApi$PostSetPassword, `plain` is not nullable")
      }

      if (!missing(`verify`) && is.null(`verify`)) {
        stop("Invalid value for `verify` when calling CrxApi$PostSetPassword, `verify` is not nullable")
      }

      query_params[["old"]] <- `old`

      query_params[["plain"]] <- `plain`

      query_params[["verify"]] <- `verify`

      local_var_url_path <- "/crx/explorer/ui/setpassword.jsp"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "character"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
