#' Adobe Experience Manager (AEM) API
#'
#' Swagger AEM is an OpenAPI specification for Adobe Experience Manager (AEM) API
#'
#' The version of the OpenAPI document: 3.7.1-pre.0
#' Contact: opensource@shinesolutions.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Granite operations
#' @description GraniteApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  SslSetup  ####################
#'
#' library(openapi)
#' var_keystore_password <- "keystore_password_example" # character | 
#' var_keystore_password_confirm <- "keystore_password_confirm_example" # character | 
#' var_truststore_password <- "truststore_password_example" # character | 
#' var_truststore_password_confirm <- "truststore_password_confirm_example" # character | 
#' var_https_hostname <- "https_hostname_example" # character | 
#' var_https_port <- "https_port_example" # character | 
#' var_privatekey_file <- File.new('/path/to/file') # data.frame |  (Optional)
#' var_certificate_file <- File.new('/path/to/file') # data.frame |  (Optional)
#'
#' api_instance <- GraniteApi$new()
#'
#' # Configure HTTP basic authorization: aemAuth
#' api_instance$api_client$username <- Sys.getenv("USERNAME")
#' api_instance$api_client$password <- Sys.getenv("PASSWORD")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SslSetup(var_keystore_password, var_keystore_password_confirm, var_truststore_password, var_truststore_password_confirm, var_https_hostname, var_https_port, privatekey_file = var_privatekey_file, certificate_file = var_certificate_filedata_file = "result.txt")
#' result <- api_instance$SslSetup(var_keystore_password, var_keystore_password_confirm, var_truststore_password, var_truststore_password_confirm, var_https_hostname, var_https_port, privatekey_file = var_privatekey_file, certificate_file = var_certificate_file)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
GraniteApi <- R6::R6Class(
  "GraniteApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new GraniteApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' 
    #'
    #' @param keystore_password 
    #' @param keystore_password_confirm 
    #' @param truststore_password 
    #' @param truststore_password_confirm 
    #' @param https_hostname 
    #' @param https_port 
    #' @param privatekey_file (optional) No description
    #' @param certificate_file (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return character
    SslSetup = function(keystore_password, keystore_password_confirm, truststore_password, truststore_password_confirm, https_hostname, https_port, privatekey_file = NULL, certificate_file = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$SslSetupWithHttpInfo(keystore_password, keystore_password_confirm, truststore_password, truststore_password_confirm, https_hostname, https_port, privatekey_file, certificate_file, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param keystore_password 
    #' @param keystore_password_confirm 
    #' @param truststore_password 
    #' @param truststore_password_confirm 
    #' @param https_hostname 
    #' @param https_port 
    #' @param privatekey_file (optional) No description
    #' @param certificate_file (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (character) with additional information such as HTTP status code, headers
    SslSetupWithHttpInfo = function(keystore_password, keystore_password_confirm, truststore_password, truststore_password_confirm, https_hostname, https_port, privatekey_file = NULL, certificate_file = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`keystore_password`)) {
        stop("Missing required parameter `keystore_password`.")
      }

      if (missing(`keystore_password_confirm`)) {
        stop("Missing required parameter `keystore_password_confirm`.")
      }

      if (missing(`truststore_password`)) {
        stop("Missing required parameter `truststore_password`.")
      }

      if (missing(`truststore_password_confirm`)) {
        stop("Missing required parameter `truststore_password_confirm`.")
      }

      if (missing(`https_hostname`)) {
        stop("Missing required parameter `https_hostname`.")
      }

      if (missing(`https_port`)) {
        stop("Missing required parameter `https_port`.")
      }

      if (!missing(`keystore_password`) && is.null(`keystore_password`)) {
        stop("Invalid value for `keystore_password` when calling GraniteApi$SslSetup, `keystore_password` is not nullable")
      }

      if (!missing(`keystore_password_confirm`) && is.null(`keystore_password_confirm`)) {
        stop("Invalid value for `keystore_password_confirm` when calling GraniteApi$SslSetup, `keystore_password_confirm` is not nullable")
      }

      if (!missing(`truststore_password`) && is.null(`truststore_password`)) {
        stop("Invalid value for `truststore_password` when calling GraniteApi$SslSetup, `truststore_password` is not nullable")
      }

      if (!missing(`truststore_password_confirm`) && is.null(`truststore_password_confirm`)) {
        stop("Invalid value for `truststore_password_confirm` when calling GraniteApi$SslSetup, `truststore_password_confirm` is not nullable")
      }

      if (!missing(`https_hostname`) && is.null(`https_hostname`)) {
        stop("Invalid value for `https_hostname` when calling GraniteApi$SslSetup, `https_hostname` is not nullable")
      }

      if (!missing(`https_port`) && is.null(`https_port`)) {
        stop("Invalid value for `https_port` when calling GraniteApi$SslSetup, `https_port` is not nullable")
      }

      if (!missing(`privatekey_file`) && is.null(`privatekey_file`)) {
        stop("Invalid value for `privatekey_file` when calling GraniteApi$SslSetup, `privatekey_file` is not nullable")
      }

      if (!missing(`certificate_file`) && is.null(`certificate_file`)) {
        stop("Invalid value for `certificate_file` when calling GraniteApi$SslSetup, `certificate_file` is not nullable")
      }

      query_params[["keystorePassword"]] <- `keystore_password`

      query_params[["keystorePasswordConfirm"]] <- `keystore_password_confirm`

      query_params[["truststorePassword"]] <- `truststore_password`

      query_params[["truststorePasswordConfirm"]] <- `truststore_password_confirm`

      query_params[["httpsHostname"]] <- `https_hostname`

      query_params[["httpsPort"]] <- `https_port`

      file_params["privatekeyFile"] <- httr::upload_file(`privatekey_file`)
      file_params["certificateFile"] <- httr::upload_file(`certificate_file`)
      local_var_url_path <- "/libs/granite/security/post/sslSetup.html"
      # HTTP basic auth
      if (!is.null(self$api_client$username) || !is.null(self$api_client$password)) {
        header_params["Authorization"] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$api_client$username, self$api_client$password, sep = ":"))))
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list("multipart/form-data")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "character"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
