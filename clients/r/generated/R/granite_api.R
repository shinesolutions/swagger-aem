# Adobe Experience Manager (AEM) API
#
# Swagger AEM is an OpenAPI specification for Adobe Experience Manager (AEM) API
#
# The version of the OpenAPI document: 3.5.0-pre.0
# Contact: opensource@shinesolutions.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Granite operations
#' @description openapi.Granite
#' @format An \code{R6Class} generator object
#' @field apiClient Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ SslSetup } \emph{  }
#' 
#'
#' \itemize{
#' \item \emph{ @param } keystore.password character
#' \item \emph{ @param } keystore.password.confirm character
#' \item \emph{ @param } truststore.password character
#' \item \emph{ @param } truststore.password.confirm character
#' \item \emph{ @param } https.hostname character
#' \item \emph{ @param } https.port character
#' \item \emph{ @param } privatekey.file data.frame
#' \item \emph{ @param } certificate.file data.frame
#'
#'
#' \item status code : 0 | Default response
#'
#' \item return type : character 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  SslSetup  ####################
#'
#' library(openapi)
#' var.keystore.password <- 'keystore.password_example' # character | 
#' var.keystore.password.confirm <- 'keystore.password.confirm_example' # character | 
#' var.truststore.password <- 'truststore.password_example' # character | 
#' var.truststore.password.confirm <- 'truststore.password.confirm_example' # character | 
#' var.https.hostname <- 'https.hostname_example' # character | 
#' var.https.port <- 'https.port_example' # character | 
#' var.privatekey.file <- File.new('/path/to/file') # data.frame | 
#' var.certificate.file <- File.new('/path/to/file') # data.frame | 
#'
#' api.instance <- GraniteApi$new()
#'
#' #Configure HTTP basic authorization: aemAuth
#' # provide your username in the user-serial format
#' api.instance$apiClient$username <- '<user-serial>'; 
#' # provide your api key generated using the developer portal
#' api.instance$apiClient$password <- '<api_key>';
#'
#' result <- api.instance$SslSetup(var.keystore.password, var.keystore.password.confirm, var.truststore.password, var.truststore.password.confirm, var.https.hostname, var.https.port, privatekey.file=var.privatekey.file, certificate.file=var.certificate.file)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
GraniteApi <- R6::R6Class(
  'GraniteApi',
  public = list(
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    SslSetup = function(keystore.password, keystore.password.confirm, truststore.password, truststore.password.confirm, https.hostname, https.port, privatekey.file=NULL, certificate.file=NULL, ...){
      apiResponse <- self$SslSetupWithHttpInfo(keystore.password, keystore.password.confirm, truststore.password, truststore.password.confirm, https.hostname, https.port, privatekey.file, certificate.file, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    SslSetupWithHttpInfo = function(keystore.password, keystore.password.confirm, truststore.password, truststore.password.confirm, https.hostname, https.port, privatekey.file=NULL, certificate.file=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`keystore.password`)) {
        stop("Missing required parameter `keystore.password`.")
      }

      if (missing(`keystore.password.confirm`)) {
        stop("Missing required parameter `keystore.password.confirm`.")
      }

      if (missing(`truststore.password`)) {
        stop("Missing required parameter `truststore.password`.")
      }

      if (missing(`truststore.password.confirm`)) {
        stop("Missing required parameter `truststore.password.confirm`.")
      }

      if (missing(`https.hostname`)) {
        stop("Missing required parameter `https.hostname`.")
      }

      if (missing(`https.port`)) {
        stop("Missing required parameter `https.port`.")
      }

      queryParams['keystorePassword'] <- keystore.password

      queryParams['keystorePasswordConfirm'] <- keystore.password.confirm

      queryParams['truststorePassword'] <- truststore.password

      queryParams['truststorePasswordConfirm'] <- truststore.password.confirm

      queryParams['httpsHostname'] <- https.hostname

      queryParams['httpsPort'] <- https.port

      body <- list(
        "privatekeyFile" = httr::upload_file(privatekey.file),
        "certificateFile" = httr::upload_file(certificate.file)
      )

      urlPath <- "/libs/granite/security/post/sslSetup.html"
      # HTTP basic auth
      headerParams['Authorization'] <- paste("Basic", base64enc::base64encode(charToRaw(paste(self$apiClient$username, self$apiClient$password, sep=":"))))

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "character", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    }
  )
)
