//
// GraniteAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech

import Foundation
import Combine
import OpenAPITransport


open class GraniteAPI {
    private let transport: OpenAPITransport
    public var encoder: JSONEncoder = {
        let encoder = JSONEncoder()
        encoder.dateEncodingStrategy = .formatted(OpenISO8601DateFormatter())
        return encoder
    }()
    public var decoder: JSONDecoder = {
        let decoder = JSONDecoder()
        decoder.dateDecodingStrategy = .formatted(OpenISO8601DateFormatter())
        return decoder
    }()
    public var baseURL = URL(string: "http://localhost")

    public init(_ transport: OpenAPITransport) {
        self.transport = transport
    }


    /// - POST /libs/granite/security/post/sslSetup.html
    /// - BASIC:
    /// - type: http
    /// - name: aemAuth
    /// - parameter keystorePassword: (query)  
    /// - parameter keystorePasswordConfirm: (query)  
    /// - parameter truststorePassword: (query)  
    /// - parameter truststorePasswordConfirm: (query)  
    /// - parameter httpsHostname: (query)  
    /// - parameter httpsPort: (query)  
    /// - parameter privatekeyFile: (form)  (optional)
    /// - parameter certificateFile: (form)  (optional)
    /// - returns: AnyPublisher<String, Error> 
    open func sslSetup(keystorePassword: String, keystorePasswordConfirm: String, truststorePassword: String, truststorePasswordConfirm: String, httpsHostname: String, httpsPort: String, privatekeyFile: Data? = nil, certificateFile: Data? = nil) -> AnyPublisher<String, Error> {
        Deferred {
            Result<URLRequest, Error> {
                guard let baseURL = self.transport.baseURL ?? self.baseURL else {
                    throw OpenAPITransportError.badURLError()
                }
                let localVarPath = "/libs/granite/security/post/sslSetup.html"
                let localVarURL = baseURL.appendingPathComponent(localVarPath)
                var components = URLComponents(url: localVarURL, resolvingAgainstBaseURL: false)
                var queryItems: [URLQueryItem] = []
                queryItems.append(URLQueryItem(name: "keystorePassword", value: keystorePassword))
                queryItems.append(URLQueryItem(name: "keystorePasswordConfirm", value: keystorePasswordConfirm))
                queryItems.append(URLQueryItem(name: "truststorePassword", value: truststorePassword))
                queryItems.append(URLQueryItem(name: "truststorePasswordConfirm", value: truststorePasswordConfirm))
                queryItems.append(URLQueryItem(name: "httpsHostname", value: httpsHostname))
                queryItems.append(URLQueryItem(name: "httpsPort", value: httpsPort))
                components?.queryItems = queryItems
                guard let requestURL = components?.url else {
                    throw OpenAPITransportError.badURLError()
                }
                var request = URLRequest(url: requestURL)
                request.httpMethod = "POST"
                let multipartBoundary = String(format: "Boundary+%08X%08X", arc4random(), arc4random())
                var multipartData = Data()
				if let privatekeyFile = privatekeyFile {
					let privatekeyFileHeader = "--\(multipartBoundary)\r\n"
					    .appending("Content-Disposition:form-data; name=\"privatekeyFile\"; filename=\"privatekeyFile\"\r\n")
					    .appending("\r\n")
					multipartData.append(privatekeyFileHeader.data(using: .utf8) ?? Data())
					multipartData.append(privatekeyFile)
					multipartData.append("\r\n".data(using: .utf8) ?? Data())
				}

				if let certificateFile = certificateFile {
					let certificateFileHeader = "--\(multipartBoundary)\r\n"
					    .appending("Content-Disposition:form-data; name=\"certificateFile\"; filename=\"certificateFile\"\r\n")
					    .appending("\r\n")
					multipartData.append(certificateFileHeader.data(using: .utf8) ?? Data())
					multipartData.append(certificateFile)
				}

                multipartData.append("\r\n--\(multipartBoundary)--\r\n".data(using: .utf8) ?? Data())
                request.httpBody = multipartData
                request.setValue("\(multipartData.count)", forHTTPHeaderField: "Content-Length")
                request.setValue("multipart/form-data; boundary=\(multipartBoundary)", forHTTPHeaderField: "Content-Type")
                return request
            }.publisher
        }.flatMap { request -> AnyPublisher<String, Error> in 
            return self.transport.send(request: request)
                .tryMap { response in
                    try self.decoder.decode(String.self, from: response.data)
                }
                .eraseToAnyPublisher()
        }.eraseToAnyPublisher()
    }
}
